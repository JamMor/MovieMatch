name: S3 Test

on:
  workflow_dispatch:

jobs:
  tester:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3
    
    - name: AWSThere
      run: aws --version
    
    - name: Echo directory
      run: echo $(ls)
      
    #- name: AWSSync
    #  run: aws s3 sync ./YOUR-BUILD-DIR s3://YOUR-BUCKET-NAME
